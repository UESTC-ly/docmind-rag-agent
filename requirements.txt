# ── Web 框架 ──────────────────────────────
fastapi
uvicorn[standard]
python-multipart          # FastAPI 文件上传必需

# ── 数据校验 / 配置 ───────────────────────
pydantic[email]
pydantic-settings
python-dotenv

# ── 数据库 ───────────────────────────────
sqlalchemy
alembic                    # 生产数据库 schema 迁移（替代启动时 create_all）
asyncpg                   # 异步驱动（FastAPI）
psycopg2-binary           # 同步驱动（Celery）
greenlet                  # SQLAlchemy async 依赖

# ── 鉴权 ─────────────────────────────────
passlib[bcrypt]
bcrypt==4.0.1             # 钉死：passlib 1.7.4 与 bcrypt 5.x 不兼容
python-jose[cryptography]
# cryptography 49.0.0 wheel exits with SIGILL on the supported Colima arm64
# production-smoke runtime; 46.0.3 is verified on host and container.
cryptography==46.0.3

# ── 异步任务 ─────────────────────────────
celery
redis

# ── 文档解析 ─────────────────────────────
pymupdf                   # PDF
python-docx               # Word

# ── AI / 向量 ────────────────────────────
openai
qdrant-client

# ── Agent 技能 ───────────────────────────
ddgs                      # DuckDuckGo 联网搜索
playwright==1.61.0        # 受控 Browser adapter；部署后按需安装 Chromium runtime
langgraph==1.2.9           # v3 外层 Agent 状态图 / interrupt / durable execution
langgraph-checkpoint-sqlite==3.1.0  # Web/桌面本地持久化 checkpoint

# ── 日志 ─────────────────────────────────
loguru

# ── 测试（开发依赖）──────────────────────
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
aiosqlite==0.22.1
httpx==0.28.1
